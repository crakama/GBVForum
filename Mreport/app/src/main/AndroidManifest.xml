<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dadaabs.mreport">

    <!-- <uses-permission android:name="android.permission.INTERNET" /> -->
    <!-- <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> -->
    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> -->


    <!-- <permission android:name="com.dadaabs.mreport.permission.MAPS_RECEIVE" -->
    <!-- android:protectionLevel="signature"/> -->
    <!-- <uses-permission android:name="com.dadaabs.mreport.permissions.MAPS_RECEIVE"/> -->
    <!-- <permission android:name="com.google.android.providers.gsf.permissions.READ_GSERVICES"/> -->


    <!-- &lt;!&ndash; -->
    <!-- The ACCESS_COARSE/FINE_LOCATION permissions are not required to use -->
    <!-- Google Maps Android API v2, but you must specify either coarse or fine -->
    <!-- location permissions for the 'MyLocation' functionality. -->
    <!-- &ndash;&gt; -->
    <!-- <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/> -->
    <!-- <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <permission
        android:name="com.dadaabs.mreport.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.dadaabs.mreport.permission.MAPS_RECEIVE" />
    <uses-permission android:name="com.google.android.providers.gsf.permissions.READ_GSERVICES" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="string"
                android:value="AIzaSyA6ZWz4JoVoS-CK5i-z-glbCPYqz-twV-U" />
        </activity>
        <activity android:name=".ReportIncidentActivity" />
        <activity android:name=".FullScreenImageActivity" />
        <activity android:name=".LoginActivity"></activity>
    </application>

</manifest>